package com.example.playbookProjApplicationBackend.Coach;

import org.springframework.data.jpa.repository.JpaRepository;

public interface CoachPositionRepository extends JpaRepository<CoachPosition, Long> {
}
